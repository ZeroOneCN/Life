# LifeOS2 健康中心模块优化 - 说明文档

> 项目名称：LifeOS2 健康中心模块优化
> 创建日期：2026-07-04
> 依据文档：[FEASIBILITY_REPORT.md](./FEASIBILITY_REPORT.md) §3.1 健康中心拓展方向
> 当前代码版本：基于 `c:\Code\LifeOS2` 代码库

---

## 一、项目规划

### 1.1 项目目标

依据《FEASIBILITY_REPORT.md》第 3.1 章节「健康中心拓展方向」，对健康中心模块进行 4 项优化拓展（已排除「健康目标管理」一项）。

### 1.2 范围与边界

| 编号 | 优化方向 | 报告优先级 | 技术难度 | 开发量 | 是否纳入 |
|------|---------|----------|---------|--------|---------|
| 1 | 健康数据可视化增强 | P1 | 低 | 2-3周 | ✅ 纳入 |
| 2 | 健康目标管理 | P1 | 低 | 1.5-2周 | ❌ 排除（用户决定） |
| 3 | 健康报告生成 | P2 | 中 | 2-3周 | ✅ 纳入 |
| 4 | 心率/血压等更多指标 | P2 | 低 | 1.5-2周 | ✅ 纳入 |
| 5 | 睡眠记录 | P3 | 低 | 1-1.5周 | ✅ 纳入 |

### 1.3 执行顺序（用户确认：按顺序执行）

1. **优化项 1**：健康数据可视化增强（健康仪表盘 + 多维图表）
2. **优化项 2**：健康报告生成（周/月/年报告 + AI 建议 + PDF 导出）
3. **优化项 3**：心率/血压等更多指标（复用体检模板机制）
4. **优化项 4**：睡眠记录（录入 + 趋势 + 关联分析）

### 1.4 技术栈与约束

- 前端：React 18 + TypeScript 5.7 + Vite 6 + Tailwind CSS 4 + Recharts/ECharts
- 后端：Express 4 + TypeORM 0.3 + MySQL 8 + Zod
- 设计体系：Stripe 风格（Indigo 主色、7 级字体层级、Pill 按钮、卡片式布局）
- 用户隔离：所有表含 `user_id`，按用户隔离
- 金额/数值：使用 decimal(12,2)（如适用）
- 函数级注释：所有新增函数必须包含功能描述、参数说明、返回值类型及用途
- Modal 关闭：仅通过显式关闭按钮（无外部点击/ESC 关闭）
- 列表操作：编辑/删除/详情使用 SVG 图标按钮 + tooltip
- 表单按钮：必须显式 `type="button"` 防止意外提交

### 1.5 健康模块现状（基线）

**前端结构：**
- 页面：[Step.tsx](./client/src/pages/health/Step.tsx)、[Fitness.tsx](./client/src/pages/health/Fitness.tsx)、[Checkup.tsx](./client/src/pages/health/Checkup.tsx)、[Medication.tsx](./client/src/pages/health/Medication.tsx)
- 组件目录：[client/src/components/health/](./client/src/components/health/)（16 个组件）
- 导航配置：[navigation.tsx](./client/src/config/navigation.tsx)（健康中心含 4 个子项）

**后端结构：**
- 路由：[step.router.ts](./server/src/modules/health/step.router.ts)、[fitness.router.ts](./server/src/modules/health/fitness.router.ts)、[medication.router.ts](./server/src/modules/health/medication.router.ts)、[checkup.router.ts](./server/src/modules/health/checkup.router.ts)
- 实体：18 个实体表，位于 [server/src/modules/health/entities/](./server/src/modules/health/entities/)
- 路由注册：[routes/index.ts](./server/src/routes/index.ts)（路径前缀 `/health/*`）

**已具备的能力：**
- `query_health` AI 工具已接入
- 通知场景：`medication.dose_reminder`、`medication.stock_low`、`checkup.followup_reminder`、`checkup.abnormal_alert`
- Telegram 快捷指令：步数、体重、饮食、运动、用药

---

## 二、实施方案

### 2.1 优化项 1：健康数据可视化增强

**目标：** 提供健康中心全局视图，跨子模块数据综合展示与多维图表分析。

**功能点：**
- 新增「健康仪表盘」页面（作为健康中心首页入口）
- 多维数据综合卡片：步数、体重、运动、用药、体检的关键指标
- 图表类型扩展：热力图（步数日历）、雷达图（综合健康度）、面积图、散点图
- 数据对比：同比/环比、目标对比、平均值对比
- 数据钻取：从汇总图点击查看明细

**后端实施：**
- 新增 `health/dashboard.router.ts`
- API 设计：
  - `GET /api/health/dashboard/overview` — 综合概览（步数/体重/运动/用药汇总）
  - `GET /api/health/dashboard/step-heatmap?year=2026` — 步数热力图数据
  - `GET /api/health/dashboard/comparison?type=step|weight|exercise` — 同比/环比对比
  - `GET /api/health/dashboard/radar` — 综合健康度雷达图数据

**前端实施：**
- 新增页面 `client/src/pages/health/HealthDashboard.tsx`
- 新增组件：
  - `HealthOverviewSection.tsx` — 综合概览卡片
  - `HealthHeatmapSection.tsx` — 步数热力图（ECharts）
  - `HealthRadarSection.tsx` — 综合健康度雷达图
  - `HealthComparisonSection.tsx` — 数据对比图表
- 导航调整：在健康中心菜单顶部新增「健康概览」入口
- 图表配色复用 [chartPalette.ts](./client/src/lib/chartPalette.ts)

**完成定义：**
- 健康仪表盘页面可访问，展示 5 类核心指标
- 热力图、雷达图、对比图正常渲染
- 数据钻取可跳转到对应子模块明细页

---

### 2.2 优化项 2：健康报告生成

**目标：** 周/月/年度健康报告自动生成，含 AI 健康建议与 PDF 导出。

**功能点：**
- 周/月/年度健康报告页面
- 数据汇总：步数、体重、运动、用药、体检等多维聚合
- 趋势分析：同比、环比变化
- 异常提醒：识别异常指标并突出展示
- AI 健康建议：基于 DeepSeek 生成个性化建议
- PDF 导出：复用 jspdf + html2canvas

**后端实施：**
- 新增 `health/health-report.router.ts`
- API 设计：
  - `GET /api/health/report/summary?period=week|month|year&date=2026-07` — 报告数据聚合
  - `GET /api/health/report/abnormal?period=month&date=2026-07` — 异常指标识别
  - `POST /api/health/report/ai-suggestion` — AI 健康建议生成（调用 DeepSeek）
- 数据来源：跨子模块聚合查询（步数/体重/运动/用药/体检）

**前端实施：**
- 新增页面 `client/src/pages/health/HealthReport.tsx`
- 新增组件：
  - `HealthReportHeaderSection.tsx` — 报告头部（周期切换）
  - `HealthReportSummarySection.tsx` — 数据汇总卡片
  - `HealthReportTrendSection.tsx` — 趋势分析图表
  - `HealthReportAbnormalSection.tsx` — 异常指标展示
  - `HealthReportAISuggestionSection.tsx` — AI 建议展示
  - `HealthReportExportButton.tsx` — PDF 导出按钮
- 导航新增「健康报告」入口（参考财务月报模式）
- 复用现有 PDF 导出工具（参考财务报告）

**完成定义：**
- 周/月/年报告可生成，数据准确
- AI 建议调用成功且合理
- PDF 导出可用，排版清晰

---

### 2.3 优化项 3：心率/血压等更多指标

**目标：** 复用体检模块的模板机制，新增日常体征记录（心率、血压、血糖、血氧等）。

**功能点：**
- 心率、血压、血糖、血氧等生命体征记录
- 复用体检模板机制（指标 + 阈值 + 异常洞察）
- 异常阈值告警
- 趋势分析

**后端实施：**
- 复用 `health-checkup-template.entity.ts` 等实体
- 新增日常体征记录类型：在体检模板中新增 `type: 'daily_vital' | 'checkup'` 区分
- 新增 API：
  - `GET /api/health/vital/records` — 日常体征记录列表
  - `POST /api/health/vital/records` — 录入日常体征
  - `GET /api/health/vital/trend?metric=heart_rate|blood_pressure` — 趋势分析
  - `GET /api/health/vital/abnormal` — 异常识别

**前端实施：**
- 新增页面 `client/src/pages/health/Vital.tsx`
- 新增组件：
  - `VitalEntrySection.tsx` — 体征录入表单
  - `VitalTrendSection.tsx` — 趋势图表（折线图）
  - `VitalAbnormalSection.tsx` — 异常指标提示
- 导航新增「日常体征」入口

**完成定义：**
- 心率、血压、血糖、血氧等指标可录入
- 阈值告警正常工作
- 趋势图表正常显示

---

### 2.4 优化项 4：睡眠记录

**目标：** 睡眠数据录入、趋势分析、与步数/体重的关联分析。

**功能点：**
- 手动录入睡眠时间（就寝/起床）、质量评分
- 睡眠趋势分析（时长、质量）
- 睡眠与步数/体重的关联分析
- 睡眠提醒（就寝/起床）

**后端实施：**
- 新增实体 `health-sleep-record.entity.ts`（字段：bedtime、wake_time、duration_minutes、quality_score、notes）
- 新增路由 `health/sleep.router.ts`
- API 设计：
  - `GET /api/health/sleep/records` — 记录列表
  - `POST /api/health/sleep/records` — 录入睡眠
  - `GET /api/health/sleep/trend?period=week|month` — 趋势分析
  - `GET /api/health/sleep/correlation` — 与步数/体重关联分析
- 新增通知场景：`sleep.bedtime_reminder`、`sleep.wake_reminder`

**前端实施：**
- 新增页面 `client/src/pages/health/Sleep.tsx`
- 新增组件：
  - `SleepEntryForm.tsx` — 睡眠录入表单
  - `SleepTrendSection.tsx` — 趋势图表
  - `SleepCorrelationSection.tsx` — 关联分析图表
- 导航新增「睡眠记录」入口

**完成定义：**
- 睡眠数据可录入（就寝时间、起床时间、质量评分）
- 趋势分析正常
- 关联分析图表展示与步数/体重的关系

---

## 三、进度记录

### 3.1 任务清单（按顺序执行）

| # | 任务 | 状态 | 完成时间 | 说明 |
|---|------|------|---------|------|
| 1 | 创建「说明文档.md」 | ✅ 已完成 | 2026-07-04 | 项目管理载体已建立 |
| 2 | 调研健康模块现状 | ✅ 已完成 | 2026-07-04 | 前后端结构梳理 |
| 3 | 优化项1：健康数据可视化增强 | ✅ 已完成 | 2026-07-04 | 健康仪表盘 + 多维图表（commit 410e2a4） |
| 4 | 优化项2：健康报告生成 | ✅ 已完成 | 2026-07-04 | 周/月/年报告 + AI建议 + PDF（commit 05e3761） |
| 5 | 优化项3：心率/血压等指标 | ✅ 已完成 | 2026-07-04 | 日常体征模块（心率/血压/血氧/血糖/体温） |
| 6 | 优化项4：睡眠记录 | ⏳ 待开始 | - | 录入 + 趋势 + 关联分析 |
| 7 | 全部完成后 push Git | ⏳ 待开始 | - | 提交所有变更 |

### 3.2 变更日志

- **2026-07-04**：创建说明文档，明确 4 项优化范围、顺序与实施方案
- **2026-07-04**：完成优化项1（健康数据可视化增强），新增 4 个聚合 API、HealthDashboard 页面与 4 个组件，已提交 commit `410e2a4`
- **2026-07-04**：完成优化项2（健康报告生成），新增 3 个后端 API（summary/abnormal/ai-suggestion，含 DeepSeek JSON 模式 + Token 计费）、HealthReport 页面与 6 个 Section 组件，复用 travel 模块 PDF 导出工具，commit 05e3761
- **2026-07-04**：完成优化项3（心率/血压等更多指标），新增 HealthVitalRecordEntity 实体与 6 个 API（指标/列表/录入/更新/删除/趋势/概览）、Vital 页面与 4 个 Section 组件（录入/概览/趋势/记录），内置 6 种体征与参考范围

---

## 四、风险与应对

| 风险 | 影响 | 应对策略 |
|------|------|---------|
| 跨模块数据聚合性能 | 中 | 添加合适索引，必要时缓存 |
| AI 建议质量不稳定 | 中 | 设计良好 prompt，错误降级 |
| 体检模板复用复杂度 | 中 | 先小范围试点，验证可行性 |
| 关联分析数据稀疏 | 低 | 数据不足时提示用户继续录入 |

---

*文档版本：1.0*
*最后更新：2026-07-04*
