fix(assistant-usage): 记录失败兜底 + 任何 chat 路径都入库

* /assistant/chat 所有分支（成功 / 失败 / DEEPSEEK_API_KEY 未配置）现在都会调用 recordAssistantUsage
  - 未配置 API Key 时记 status=error，用户能在「本站 AI 助理请求」看到自己的尝试
* recordAssistantUsage 增加 3 层兜底：
  1) ensureAssistantUsageTable() 用 CREATE TABLE IF NOT EXISTS 幂等建表
  2) Repository.insert 失败时回退到原生 SQL INSERT
  3) 控制台输出成功/失败日志，方便排查
* getAssistantUsageStats 任何阶段抛错都返回 EMPTY_STATS，避免组件因缺表而 500
* 前端 widget 增加 30 秒自动轮询 + 优化空状态文案
