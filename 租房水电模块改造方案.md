# LifeOS2 租房水电模块改造方案

> 项目名称：LifeOS2 — 全生命周期数字化管理平台
> 改造范围：finance 房租水电模块（`/finance/bill-mgmt?billMgmtTab=rent` 与 `/finance/rent`）
> 方案类型：全方位改造（允许功能重写、摒弃现有方式）
> 启动日期：2026-08-15
> 依据调研：Rent 前端 5 组件 + 服务层 + 类型、后端 rent.router / 4 实体、bill-aggregator / bill-reminder scheduler、bill-mgmt 嵌入集成、既有规划文档（改版方案.md / Finance改进说明文档.md）

---

## 一、现状诊断

### 1.1 前端现状

| 区块 | 现状 | 问题 |
|---|---|---|
| 页面形态 | RentPage 5 Tab：住房记录 / 录入编辑 / 统计分析 / 渠道管理 / 水电账单；embedded 模式嵌入 bill-mgmt（merged-tabs-top） | 双入口（独立页 + 聚合页）两套 Tab，录入→水电需跨 Tab 多跳 |
| 状态管理 | 全部 `useState` 集中在 RentPage，子组件靠 props 透传 + onChangeRecords/onChangeChannels 乐观更新 | 状态分散、无统一 store；水电账单组件内部自管数据，与主状态脱节 |
| 录入表单 | RentEntrySection 一屏大表单（基础信息+朝向+合同总额+支付周期+实际月租+押金+4 项杂费+备注+实时预览） | 字段多、无分组折叠；提交后回列表再切 Tab 录水电，体验割裂 |
| 住房记录 | 表格（筛选/分页 10/详情弹窗含「按月份拆分成本」）/ 编辑 / 删除 | 详情弹窗信息量大，但无「逐月实付/押金退还」追踪 |
| 水电账单 | RentUtilityBillsSection 独立 Tab，需先选 recordId | 与住房记录割裂；只记电/水/燃气金额，无抄表数/单价/已缴状态 |
| 统计分析 | 费用结构占比、费用明细、渠道分布、水电月度趋势 4 图 | 缺「月度房租/实付趋势」，未与付款数据打通 |

### 1.2 后端现状

| 区块 | 现状 | 问题 |
|---|---|---|
| API | rent.router.ts：records / channels / settings / utility-bills / overview / cost-breakdown / channel-breakdown 完整 CRUD | 无付款记录、无押金流转 API |
| 派生指标 | calculateRentMetrics：stayDays / totalCost / dailyCost / monthlyRent / quarterlyRent / occupancyStatus / payCycle / contractMonthlyRent | 前后端各一套计算（前端 calculateRentDerivedMetrics），口径有分叉风险 |
| 跨月拆分 | 仅前端 calculateRentMonthlyBreakdown 实现 | 后端无对应能力，统计口径不一致 |
| 统一账单 | bill-aggregator.transformRentBill 按入住月份动态生成房租账单 | **金额用 record.rent（合同总额），未用实际月租 rentPerMonth → 账单页房租虚高**（正是用户反馈 1800 vs 1000 的根因）；paid_amount 恒 0、status 恒 pending，无法标记已付/逾期 |
| 提醒 | bill-reminder.scheduler 已覆盖 rent（逾期/今日/明日 + 四档升级），enabled_types 默认含 rent；设置实体 finance_bill_reminder_setting | 依赖聚合口径（同上金额错）；水电账单不在统一账单内，无水电/押金提醒 |
| 水电账单 | finance_rent_utility_bill 表（year_month 唯一 + record_id 关联） | 不参与统一账单，不能标记「已缴」 |

### 1.3 核心痛点编号

- **P-01 口径错误**：统一账单/提醒中房租金额取合同总额而非实际月租（金额虚高，核心 BUG）
- **P-02 记账缺失**：只存合同总额与实际月租，无法逐月记录「实付多少、押金交没交/退没退、水电缴没缴」，不满足用户单独记账诉求
- **P-03 水电割裂**：水电账单独立 Tab，与住房记录/付款记录分离，跨 Tab 操作繁琐
- **P-04 状态分散**：无统一状态层，props 透传 + 组件自管数据混用
- **P-05 口径分叉**：前后端各一套派生指标计算
- **P-06 提醒盲区**：水电月度、押金退还无提醒；房租提醒金额错
- **P-07 无导入导出**：购物/号卡等模块有 Excel 能力，rent 无
- **P-08 无测试**：核心计算无单测，改造回归风险高

---

## 二、改造目标与原则

### 2.1 目标

把「房租水电」从**档案 + 费用登记**升级为 **「租房全生命周期管理 + 月度账单闭环」**：

1. 一个统一入口承载（bill-mgmt 聚合页），独立路由保留为完整视图
2. 逐月付款可记账（房租/水电/押金实付），支撑单独记账
3. 房租账单按**实际月租**生成 → 支持标记已付 → 到期/逾期提醒 → 统计全闭环
4. 水电账单与住房记录、付款记录打通，不再割裂
5. 前后端共用一套计算口径（含跨月拆分）

### 2.2 原则

- **可摒弃现有方式**：页面形态、交互、状态层可重写，但**数据库结构向后兼容**，现有数据（含 pay_cycle / rent_per_month）不丢
- **分阶段交付**：P0 口径修复先行（立即解决金额虚高），P1-P4 逐步重构，每阶段独立验收并推送
- **口径统一优先**：所有金额计算收敛到一处（后端 calculateRentMetrics 为唯一权威，前端仅作派生展示）
- **精确计算**：金额一律 decimal(12,2)，严禁浮点误差
- **不超范围**：不做多币种、不做房东管理 CRM、不做合同模板

---

## 三、总体设计

### 3.1 数据模型（新增 1 实体 + 扩展 2 实体）

**① 新增 `finance_rent_payment`（付款记录表，核心新增）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | PK | 主键 |
| user_id | FK→user | 用户隔离（继承 UserScopedEntity） |
| record_id | FK→rent_record | 关联住房记录 |
| month | varchar(7) | 账期 YYYY-MM |
| type | varchar(16) | rent / utility / deposit（房租/水电/押金） |
| amount | decimal(12,2) | 实付金额 |
| paid_date | date | 实际支付日期 |
| utility_bill_id | FK→utility_bill 可空 | 水电账单关联（type=utility 时） |
| notes | text | 备注 |
| created_at / updated_at | datetime | 审计 |

唯一约束：`(record_id, type, month)`，同月同类型仅一笔，冲突走 409。

**② 扩展 `finance_rent_record`（住房记录表）**

| 新增字段 | 类型 | 说明 |
|---|---|---|
| billing_day | int 默认 1 | 房租账单日（每月几号交租），驱动提醒与账单生成 |
| deposit_status | varchar(16) 默认 unpaid | unpaid / refunded / forfeited（未退/已退/没收） |
| deposit_refund_date | date 可空 | 押金退还日期 |
| deposit_notes | text 可空 | 押金流转备注 |

**③ 扩展 `finance_rent_utility_bill`（水电账单表）**

| 新增字段 | 类型 | 说明 |
|---|---|---|
| electricity_meter | decimal(12,2) 可空 | 电表读数 |
| water_meter | decimal(12,2) 可空 | 水表读数 |
| gas_meter | decimal(12,2) 可空 | 燃气表读数 |
| paid | tinyint 默认 0 | 是否已缴 |

### 3.2 后端设计

**① 修复统一账单口径（P-01，最高优先级）**
- `bill-aggregator.transformRentBill`：金额改用 `rent_per_month`（实际月租）；未填实际月租时回退按 payCycle 换算（与 calculateRentMetrics 完全一致）；`due_date` 改为「每月 billing_day」而不是固定 1 号
- 若已有付款记录：`paid_amount` 取该月实付、`status` 按实付≥应付标 paid

**② 付款 API（P-02）**
- `POST/GET/PATCH/DELETE /api/finance/rent/payments`（按 recordId / month / type 过滤）
- `GET /api/finance/rent/payments/summary`：按记录汇总押金、已缴/待缴、本月实付
- 水电账单保存时可选联动生成 `type=utility` 付款记录（paid=true）

**③ 水电账单增强（P-03）**
- utility-bill 增删改支持抄表数、已缴状态
- 可选：水电账单纳入统一账单（`transformUtilityBill`，金额=电+水+燃气，type=rent 下拆 subtype）

**④ 提醒闭环（P-06）**
- 修复 bill-reminder.scheduler 房租口径（随 ① 自动生效）
- 新增场景：
  - `finance.rent.due`：每月 billing_day 房租到期（复用现有 FINANCE_BILL_UPCOMING 场景即可，无需新场景）
  - `finance.rent.deposit`：退租后押金未退提醒（新增场景种子，退租超过 N 天未标记退还）
- 水电月结提醒：账单录入当月（可选，默认关闭）

**⑤ 跨月拆分后端化（P-05）**
- 后端提供 `GET /api/finance/rent/records/:id/monthly-breakdown`，实现与前端 calculateRentMonthlyBreakdown 等价逻辑；前端改为调用该接口（口径唯一化）

**⑥ 导入导出（P-07）**
- 水电账单 / 住房记录 Excel 导入导出，复用 shopping/card 模块既有导入工具链

### 3.3 前端设计（页面形态重构）

**新页面形态（bill-mgmt 聚合页内嵌 rent，独立路由共用同一组件树）：**

```
房租水电（RentTab）
├─ 概览      在住房卡片、下月待付房租、押金总额、本月水电实付、最近付款列表
├─ 住房记录   表格（含 下月应付/押金状态 列）+ 详情弹窗
│             └─ 详情弹窗内：基础信息 + 跨月拆分 + 付款记录 + 水电月度内联操作
├─ 水电账单   独立视图（多记录月度汇总），入口保留
├─ 统计分析   保留 4 图 + 新增「月度实付趋势」「押金流转」口径
└─ 设置      渠道管理 + 房租账单日/押金/提醒配置
```

**关键重构点：**

1. **水电内联**：住房记录详情弹窗内直接按月录入水电，保存后联动付款记录，无需切 Tab（P-03）
2. **付款记录**：详情弹窗内「逐月实付」区块，类型选择（房租/水电/押金），自动带出应付金额建议（P-02）
3. **录入表单分组**：RentEntrySection 拆为「基础信息 / 费用与支付 / 押金」三个分组卡片，实时计算保留（符合用户不折叠偏好 → 用分组卡片而非折叠）
4. **状态管理**：引入轻量 rent store（Zustand，与 workspace.store 一致），统一 records/channels/utilityBills/payments，删除 RentPage 全量 props 透传（P-04）
5. **embedded 集成优化**：bill-mgmt 切到 rent 默认进入「概览」；主 Tab 切换清参逻辑保留；独立路由与聚合页复用同一组件树，避免分叉
6. **口径前端化**：前端派生指标全部由后端 calculateRentMetrics 返回驱动，前端只格式化展示（P-05）

### 3.4 交互与体验（对齐用户偏好）

- 概览卡突出关键财务结果（下月待付、押金、本月水电）——高亮展示
- 列表操作沿用 SVG 图标按钮 + tooltip（编辑/详情/删除）
- 长数值列预留足够宽度，金额用 title 悬浮显示全量
- 加载保留旧数据 + opacity 视觉提示，避免抖动
- 所有 Modal 仅显式关闭按钮关闭；表单按钮显式 type="button"
- 水电/付款录入支持「保存后自动跳到下一个月」便于连续录入

---

## 四、分阶段实施计划

| 阶段 | 内容 | 依赖 | 目标完成 | 验收 |
|---|---|---|---|---|
| **P0 口径修复（快赢）** | ① 修复 transformRentBill 用实际月租+billing_day；② 水电账单纳入统一账单（可关）；③ 前端折算口径对齐后端 | 无 | 2026-08-15 | 统一账单页房租金额=实际月租；现有数据不受影响 |
| **P1 数据模型与付款后端** | ① 新增 finance_rent_payment 实体+迁移；② 扩展 record/utility_bill 字段+迁移；③ 付款 CRUD + summary API；④ 水电账单联动付款 | P0 | 2026-08-16 | 付款记录可增删改查；水电已缴状态生效；typecheck/check 零错误 |
| **P2 前端重构** | ① rent store（Zustand）；② 页面形态重构（概览/记录/水电/统计/设置）；③ 水电内联详情弹窗；④ 录入表单分组；⑤ 详情弹窗付款区块 + 跨月拆分后端化 | P1 | 2026-08-17~18 | 5 区块可用；无 props 透传残留；embedded 与独立路由一致 |
| **P3 提醒闭环** | ① 房租到期提醒随 P0 生效；② 押金退还提醒场景种子；③ 水电月结提醒（默认关）；④ 退租后押金状态流转 | P1 | 2026-08-19 | 通知中心可见对应场景；手动触发 scheduler 验证 |
| **P4 增强收尾** | ① Excel 导入导出（水电/记录）；② 统计新增月度实付趋势/押金流转；③ 核心计算单测；④ 全量回归 + 更新说明文档 + 推送 | P2 | 2026-08-20 | 全部验收项通过，提交推送 origin/master |

> 说明：每阶段完成后立即在「说明文档.md」进度记录中标记完成并补充结果说明；计划调整时同步更新本文档与说明文档；任何阶段出现错误立即暂停按问题处理规范修复并验证后再推进。

---

## 五、边界与约束（明确不做什么）

- 不新增「房东/合同管理 CRM」能力
- 不做多币种、汇率折算
- 不改动 finance 其他子模块（贷款/订阅/购物/旅行）核心逻辑
- 数据库迁移向前兼容，现有数据（pay_cycle / rent_per_month / 水电账单）完整保留
- 通知渠道配置归属通知中心，本模块仅注册场景，不实现渠道逻辑

---

## 六、风险与规避

| 风险 | 规避措施 |
|---|---|
| P0 改口径导致历史账单金额变化 | 属**修复而非破坏**；旧数据实际月租为空时按 payCycle 回退，与用户真实支付一致；推送前确认 |
| 数据库迁移影响现有数据 | 新增表/新增列均带默认值；synchronize 由 env 控制，迁移前备份 |
| 前后端口径分叉 | 后端 calculateRentMetrics 为唯一权威，前端只读接口结果；跨月拆分后端化 |
| 双入口分叉（独立页 vs 聚合页） | 两入口渲染同一组件树，仅外层容器不同 |
| 重构回归 | P0 先行落地核心修复；P2 采用组件级替换 + typecheck + 功能走查；P4 补单测兜底 |

---

## 七、验收标准（总）

1. 统一账单页房租金额 = 实际月租（或按支付周期回退），不再出现合同总额虚高
2. 支持逐月记录房租/水电/押金实付，详情弹窗可查付款流水与押金状态
3. 水电账单可在住房详情内联操作，并联动付款记录；独立视图保留
4. 房租到期/逾期提醒金额准确；押金退还提醒可触发
5. 前后端计算口径唯一，跨月拆分前后一致
6. 前端 5 区块 + embedded 集成 + 独立路由均可用，typecheck/check 零错误
7. 说明文档.md 进度完整记录，全部改动推送 origin/master
